apiVersion: v1
kind: Service

metadata:
  name: fraud-service
  namespace: banking-platform

spec:
  type: ClusterIP

  selector:
    app: fraud-service

  ports:
    - port: 8082
      targetPort: 8082