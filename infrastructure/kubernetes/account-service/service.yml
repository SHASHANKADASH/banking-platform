apiVersion: v1
kind: Service

metadata:
  name: account-service
  namespace: banking-platform

spec:
  type: ClusterIP

  selector:
    app: account-service

  ports:
    - port: 8080
      targetPort: 8080